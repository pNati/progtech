<Page x:Class="WpfBurgerApp.Views.OrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Fizetés" Height="200" Width="300">

    <Grid Margin="20">
        <Button Content="Vissza a főoldalra"
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom"
            Margin="-6,0,0,-8"
            Click="BackButton_Click"
            Background="Aquamarine"
            Width="106" />

        <Button Content="Fizetés"
             HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,97,0,0"
            Click="BackButton_Click"
            Background="Green"
            Width="120" />

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">



            </StackPanel>
        </StackPanel>
        <TextBox TextWrapping="Wrap" Text="Végleges rendelésem" Width="120" TextChanged="TextBox_TextChanged" Margin="70,0,70,137"/>
        <TextBox HorizontalAlignment="Center" Margin="0,56,0,0" TextWrapping="Wrap" Text="Összeg: " VerticalAlignment="Top" Width="120"/>
    </Grid>
</Page>
